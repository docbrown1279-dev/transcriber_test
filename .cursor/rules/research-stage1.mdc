---
description: Этап 1f — ONNX диаризация на 4 eval-клипах; эталон pyannote 1e; ASR полного файла заморожен
alwaysApply: true
---

# Этап 1f — лёгкие аннотаторы

- План: `docs/research_plan.md`, промпт: `docs/prompts/stage1f_diarization.md`.
- Сначала метки vs `results/reports/1f/baseline/pyannote31/` (`scripts/stage1f_compare_turns.py`). Потом GigaAM v3 на тех же turns.
- Не запускать pyannote 3.1 заново. Не читать `eval/`. Не переписывать `results/**/2/`.
- Два ONNX: `sherpa_onnx`, `vad_wespeaker`. VAD без спикеров — не кандидат.
- Отчёты: `results/reports/1f/`. Ветка: `cursor/stage1f-onnx-diarization`.
