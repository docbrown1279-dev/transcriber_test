---
description: Stage 2 — GigaAM v3 + pyannote + gain, one rubert-tiny2 adjacent chunk pass, Qwen3-8B titles if 5–30 chunks
alwaysApply: true
---

# Stage 2 — chunks + timestamp titles

- Source: `docs/research_plan.md`, `AGENTS.md`, `docs/prompts/stage2_chunk_titles.md`.
- ASR: **GigaAM v3_rnnt + pyannote 3.1 + linear gain**. No denoise. No second ASR.
- Input: full `data/fixtures/meeting_sample.m4a`.
- Chunk: **whole speaker turns**, start ~20–50 words / cosine **0.80**, `rubert-tiny2`, adjacent only. If count ∉ [5, 30] → **≤2 more tries** (unit size + threshold only). After 3: stop.
- Titles: **Qwen3-8B** ≤10 words, only if some try is 5–30 chunks.
- `HF_TOKEN` for pyannote; **401/403 → stop**. No cloud ASR.
- Branch: `cursor/stage1e-four-asr-be20`. Reports: `results/reports/2/`.
