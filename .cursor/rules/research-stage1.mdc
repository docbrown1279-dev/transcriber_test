---
description: Этап 2b — чанкинг на готовой стенограмме GigaAM; без нового ASR, без шумодавов, без bakeoff LLM
alwaysApply: true
---

# Этап 2b — чанкинг

- Источник: `docs/research_plan.md`, `AGENTS.md`, `docs/prompts/stage2b_chunking.md`.
- ASR: **не трогать вообще**. Читать неизменный `results/asr/2/gigaam_v3_rnnt/meeting_sample.json`. Нет файла → стоп, не восстанавливать новым прогоном.
- Вход глав: `results/chunking/2/attempt_2_chunks_titled.json`.
- Путь последнего агента не повторять: не запускать скрипты ASR, три старых прохода tiny2, повторную генерацию 63 заголовков и склейки 63→4→49. Пути `results/**/2/` не перезаписывать.
- Четыре независимых опыта: A эмбеддинг названий; B попарный Qwen3-8B; C packing разных спикеров; D late chunking (`jina-embeddings-v3`, окно 240 с, overlap 60 с). Не смешивать. Цель **8–20** глав.
- Каждая склейка в логе: `source_ids`, `old_titles`, `new_title`, покрытие листьев. Не one-shot на 63 id.
- LLM не создаёт таймкоды. Для A/B `start/end` копировать из baseline-чанков, для C/D — из ASR-сегментов; деление исходного листа запрещено.
- Шумодавы **пропуск**. Разные LLM и полное саммари — **этап 3**, не 2b.
- Ветка: `cursor/stage1e-four-asr-be20`. Отчёты: `results/reports/2b/`.
