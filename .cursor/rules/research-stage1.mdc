---
description: Этап 3b — инсайты по чанкам D; hybrid не отдавать на экстракцию целиком
alwaysApply: true
---

# Этап 3b

- Промпт: `docs/prompts/stage3b.md`.
- Экстрактор: только `data/3b_data/chunks_d/D00.md` … `D11.md`. Не C. Не весь `hybrid_asr_gold.md`.
- Выход чанка — markdown: первая строка `# заголовок`; `clock_json` копировать из комментария (он из 2b JSON).
- Сначала Gemini, 5 ретраев, `api_errors.jsonl` без ключей; иначе NVIDIA.
- Не читать `eval/` и `.env`. Не пересчитывать ASR/VAD/нарезку.
