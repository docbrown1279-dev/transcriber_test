# AGENTS.md — исследование распознавания речи

## Задача

Этап **2b** (сейчас): гипотезы **чанкинга** на неизменной готовой стенограмме GigaAM v3. ASR больше не трогать. Шумодавы не трогать. Разные LLM и полное саммари — этап 3.

## Роль

Ты агент-**исследователь**. Читать и следовать:

1. [`docs/research_plan.md`](docs/research_plan.md) — карта этапов и протокол 2b
2. [`.cursor/skills/asr-research/SKILL.md`](.cursor/skills/asr-research/SKILL.md) — workflow
3. [`docs/prompts/stage2b_chunking.md`](docs/prompts/stage2b_chunking.md) — прогон без присмотра
4. [`docs/schemas/chunk_merge_log.schema.json`](docs/schemas/chunk_merge_log.schema.json) — лог склеек
5. [`docs/schemas/research_report.schema.json`](docs/schemas/research_report.schema.json) — схема отчёта

Этап 2 (полный файл + три прохода tiny2 + 63 заголовка + склейки 4/49) закрыт. Не запускать заново `stage2_chunk_titles.md`, `run_stage2_asr.py`, `chunk_stage2.py`, `title_stage2.py`, `merge_titles_stage2.py` и `refine_title_merge_stage2.py`. Артефакты этапа 2 не перезаписывать.

## Текущий этап (2b)

| В скоупе | Вне скоупа |
|---|---|
| Неизменная стенограмма `results/asr/2/gigaam_v3_rnnt/meeting_sample.json` | Любой ASR, pyannote, gain, обработка аудио, исправление стенограммы |
| Эмбеддинг названий, попарный Qwen3-8B, packing разных спикеров, **late chunking** | Сравнение разных LLM (этап 3) |
| Лог склейки: `source_ids`, старые/новые заголовки, покрытие листьев | One-shot JSON на 60+ id |
| Таймкоды только из границ исходных ASR-сегментов | Таймкоды, сгенерированные/исправленные LLM; деление сегмента без word timestamps |
| Цель **8–20 глав** на эту запись | Четвёртый порог tiny2 «один спикер» |
| Новые артефакты только в путях `2b/` | Повтор опытов этапа 2; перезапись путей `2/` |
| Отчёт `results/reports/2b/` | Шумодавы; Whisper; полное саммари встречи; аудио в API |

## Фикстуры

| Путь | Описание |
|---|---|
| `data/fixtures/meeting_sample.m4a` | Полное совещание (~24.5 мин) — вход этапа 2; на 2b только таймкоды из JSON |
| `docs/Голос 002.m4a` | Тот же файл |
| `data/test_*.m4a` | Eval-клипы (1e); не нарезать только их |

## Секреты

| Секрет | Зачем |
|---|---|
| `HF_TOKEN` / `HUGGING_FACE_HUB_TOKEN` | На 2b не нужен (pyannote уже отработал). Если кто-то всё же дергает gated файлы: **401/403 → стоп** |
| Gemini / NVIDIA | Не использовать в 2b |

Секреты не печатать и не коммитить. Золото `eval/` не коммитить.

## Критерий успеха 2b

Сделать четыре **независимых** кандидата A–D, включая late chunking. Для каждого — главы в **8–20** (если метод способен), полное покрытие листьев и точные времена из исходного JSON. Победителя без человеческой проверки не выбирать. Этап 3 не начинать.

## Жёсткие лимиты

| Блок | Потолок |
|---|---|
| ASR | **0** новых прогонов и **0** изменений текста; только читать готовый JSON |
| Эмбеддеры | tiny2 для A/C + **1** long-context модель для D (`jina-embeddings-v3`) |
| Опыты чанкинга | A, B, C, D — четыре независимых кандидата, не смешивать |
| LLM | **1**: Qwen3-8B, только заголовок/пара; не bakeoff и не полное саммари |
| Установка пакетов | Не устанавливать без явного разрешения пользователя |

Ошибка (crash, OOM): тот же конфиг ещё до **2** раз. Промах по числу глав — не повод крутить четвёртый порог tiny2 «один спикер».

## Без присмотра

Ставить пакеты не дожидаясь. Коммит/push **`cursor/stage1e-four-asr-be20`**. Не force-push в `main`. Отчёты по-русски.

## Чеклист запуска

```
- [ ] Прочитать docs/research_plan.md + этот файл + промпт 2b
- [ ] Проверить results/asr/2/ и attempt_2_chunks_titled.json; нет файлов → стоп
- [ ] Опыт A (эмбеддинг заголовков, ≤3 порога); лог склейки
- [ ] Опыт B (попарный Qwen), независимо от A
- [ ] Опыт C (packing разных спикеров), независимо от A/B
- [ ] Опыт D (late chunking: окно 240 с / overlap 60 с / главы 45–180 с)
- [ ] Для A–D проверить времена по исходным segment ids; подготовить review_sheet
- [ ] results/reports/2b/; коммит/push cursor/stage1e-four-asr-be20
```
