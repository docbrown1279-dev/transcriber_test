Future purpose folder (Q&A / interview template). Not used in demo D3.

Add versioned files here later (`v1.md`, `v2_short.md`, …) and point a new
`llm.tasks.qa_session` block at them. Do not reuse `meeting_insights` prompts in-place.
