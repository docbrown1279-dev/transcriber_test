# Draft — H4 constrained rebuild (final split diar spike)

**Status:** READY to paste/push  
**Beat:** H1 (`split3_cluster_rebuild`)  
**Then:** stop-line → product path; no more assign zoos

## Why full ≠ split

| Full file | Split TTFT |
|---|---|
| One AHC on all windows | Part1 locked early for UI |
| Global blobs for main talkers | Part2 local structure ≠ part1 centroids |
| False micro-ids 03/04 still happen | Window-assign flickered; H1 fixed flicker, risk oversmooth |

So full “worked” on continuity of big speakers, not because assign-to-gallery is wrong forever — because **global cluster geometry** is stronger than matching a 5′ tail to a 5′ gallery.

## H4 (this experiment)

Rebuild on the union of windows, **constraint:** part1 labels unchanged (no cluster may mix two part1 speakers). ≤20 attempts (threshold grid). Fallback: Lloyd, part1 frozen, part2 init=H1, ≤20 iters.

New-id≥2s does **not** glue 00↔01; H4 might, by re-cutting blobs under the part1 anchor.

## After cloud

Read `compare.md` → choose **H1 or H4** → backlog the rest → move on (port split / next ticket).
