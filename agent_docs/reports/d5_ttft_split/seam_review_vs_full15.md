# Стыки split3 vs full15 — выборка и сверка

Baseline = **unsplit full15** (тот же 15′ файл). Hyp = **merged split3**.

## Стата по частям

| part | n_base | matched | unmatched | mean text Jaccard | share Jaccard≥0.7 | speaker agree (all base) | speaker agree (matched only) | mean IoU |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| part01 | 34 | 34 | 0 | 0.998 | 1.0 | 1.0 | 1.0 | 0.996 |
| part02 | 56 | 55 | 1 | 0.642 | 0.554 | 0.661 | 0.673 | 0.718 |
| part03 | 53 | 53 | 0 | 0.717 | 0.679 | 0.906 | 0.906 | 0.825 |

Текст и спикеры — **раздельные** метрики: Jaccard по словам; speaker agree = доля baseline-сегментов, у которых matched hyp с тем же `SPEAKER_*`.

> На full15 своя AHC (5 id), на split3 — gallery с part1 (3 id). Расхождение спикеров часто = другой кластерный ярлык, не потеря речи.

## Выборка у стыка 291.4s

Пауза плана: 290.71–292.08 (длительность 1.37s), mid=291.39.

### 1. baseline `s0037` 301.4–323.9 `SPEAKER_00`

- **baseline:** вы понимаете понимаете воду вы наверное понимаете помещение кп заявок подавали у вас все есть связь понятно прокладываете что еще там тепло помещение кп у вас
- **merged:** — нет пары (unmatched)

### 2. baseline `s0036` 300.7–301.4 `SPEAKER_01`

- **baseline:** да
- **merged:** `s0036` 297.6–304.4 `SPEAKER_00`
- **merged text:** да вы понимаете понимаете
- text Jaccard=0.333, IoU=0.11, speaker DIFF (SPEAKER_01 → SPEAKER_00)

### 3. baseline `s0035` 292.1–299.9 `SPEAKER_00`

- **baseline:** давайте сейчас второй третий пункт немножко перескочим по магистральным сетям информация у вас вся есть насколько я понимаю да
- **merged:** `s0035` 294.5–296.8 `SPEAKER_00`
- **merged text:** по магистральным сетям
- text Jaccard=0.158, IoU=0.296, speaker OK

### 4. baseline `s0031` 264.2–269.7 `SPEAKER_00`

- **baseline:** вот то есть все что мы можем и должны передать мы в принципе уже передали
- **merged:** `s0031` 264.2–269.7 `SPEAKER_00`
- **merged text:** вот то есть все что мы можем и должны передать мы в принципе уже передали
- text Jaccard=1.0, IoU=1.0, speaker OK

### 5. baseline `s0032` 270.7–276.2 `SPEAKER_00`

- **baseline:** вот и соответственно мы у нас будет понимание что все
- **merged:** `s0032` 270.7–276.2 `SPEAKER_00`
- **merged text:** вот и соответственно мы у нас будет понимание что все
- text Jaccard=1.0, IoU=1.0, speaker OK

### 6. baseline `s0033` 276.7–286.0 `SPEAKER_00`

- **baseline:** ну я имею согласно договора все вот эти требования по ландшафтной концепции как примеры данных просто что ориентировать
- **merged:** `s0033` 276.7–286.0 `SPEAKER_00`
- **merged text:** ну я имею согласно договора все вот эти требования по ландшафтной концепции как примеры данных просто что ориентировать
- text Jaccard=1.0, IoU=1.0, speaker OK

### 7. baseline `s0034` 287.8–290.7 `SPEAKER_00`

- **baseline:** так о
- **merged:** `s0034` 287.8–290.7 `SPEAKER_00`
- **merged text:** так о
- text Jaccard=1.0, IoU=1.0, speaker OK

## Выборка у стыка 602.0s

Пауза плана: 601.14–602.96 (длительность 1.82s), mid=602.05.

### 1. baseline `s0092` 610.1–611.6 `SPEAKER_03`

- **baseline:** предлагает это
- **merged:** `s0104` 610.7–616.2 `SPEAKER_00`
- **merged text:** предлагает этот закончить мы каждый раз на каждом корпусе одно и тоже обсуждаем поэтому ничего нового здесь нет
- text Jaccard=0.053, IoU=0.14, speaker DIFF (SPEAKER_03 → SPEAKER_00)

### 2. baseline `s0087` 582.2–583.7 `SPEAKER_04`

- **baseline:** поменять наш наш вопрос
- **merged:** `s0100` 581.4–583.7 `SPEAKER_01`
- **merged text:** или оно еще может поменяться наше что наш
- text Jaccard=0.1, IoU=0.667, speaker DIFF (SPEAKER_04 → SPEAKER_01)

### 3. baseline `s0086` 574.7–582.2 `SPEAKER_02`

- **baseline:** количество квартир на сегодня уже будет ну как бы точно известно или оно еще может
- **merged:** `s0099` 579.2–581.4 `SPEAKER_00`
- **merged text:** будь ну как бы точно известно
- text Jaccard=0.312, IoU=0.3, speaker DIFF (SPEAKER_02 → SPEAKER_00)

### 4. baseline `s0088` 583.7–586.7 `SPEAKER_00`

- **baseline:** конечно можешь поменять
- **merged:** `s0101` 583.7–593.3 `SPEAKER_00`
- **merged text:** еще не согласовал ну вот все значит мы тогда с точки зрения инженерии ждем согласования квартиры и тогда уже
- text Jaccard=0.0, IoU=0.311, speaker OK

### 5. baseline `s0089` 586.7–593.4 `SPEAKER_02`

- **baseline:** ну вот все значит мы тогда с точки зрения инженерии ждем согласования квартиры и тогда уже
- **merged:** `s0101` 583.7–593.3 `SPEAKER_00`
- **merged text:** еще не согласовал ну вот все значит мы тогда с точки зрения инженерии ждем согласования квартиры и тогда уже
- text Jaccard=0.833, IoU=0.687, speaker DIFF (SPEAKER_02 → SPEAKER_00)

### 6. baseline `s0090` 595.0–601.1 `SPEAKER_00`

- **baseline:** понятное дело но опять же говорю вот есть у вас там три с половиной мегаватта или сколько я сейчас не помню
- **merged:** `s0102` 595.0–601.1 `SPEAKER_00`
- **merged text:** понятное дело да действие но опять же говорю вот есть у нас там триста мегаватта или сколько я сейчас не помню
- text Jaccard=0.68, IoU=1.0, speaker OK

### 7. baseline `s0093` 611.6–616.1 `SPEAKER_00`

- **baseline:** закончится мы каждый раз на каждом корпусе одно и тоже обсуждаем поэтому ничего нового здесь
- **merged:** `s0104` 610.7–616.2 `SPEAKER_00`
- **merged text:** предлагает этот закончить мы каждый раз на каждом корпусе одно и тоже обсуждаем поэтому ничего нового здесь нет
- text Jaccard=0.737, IoU=0.815, speaker OK

### 8. baseline `s0091` 603.0–607.1 `SPEAKER_00`

- **baseline:** вот больше не меньше можете сделать больше
- **merged:** `s0103` 602.9–607.2 `SPEAKER_00`
- **merged text:** вот больше не меньше можете сделать больше не будет
- text Jaccard=0.857, IoU=0.97, speaker OK

