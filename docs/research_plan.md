1. **Библиотеки:** Строго `Whisper` (база) + пара специфических для шума/плохого качества (например, `faster-whisper` с разными моделями или `whisper.cpp`). Никаких экспериментов с экзотикой типа NeMo, если только Whisper не провалится полностью.
2. **Шумодав:** Отдельный трек исследований. Не смешивать с ASR. Тестим: `DeepFilterNet`, `RNNoise`, простые фильтры `ffmpeg` и другие если есть перспективные 
3. **Качество (Метрика):**
    * База: `% русских слов >= 0.9` (стандартный словарь).
    * Список OOV (Out-of-Vocabulary): агент просто выводит список слов, которые не узнал.
    * **Human-like check:** Агент выборочно читает 3-5 фрагментов текста. Если там "ахинея из русских слов" (бред), ставит флаг `FAIL`.
    * Лимит попыток: 3 на библиотеку.
4. **Таймкоды и Семантика (Сразу!):**
    * Да, исследуем локальные эмбеддинги (`BGE-M3`, `E5`) для чанкинга по смыслу.
    * Пробуем запустить локальную LLM (`Qwen2.5-7B-Instruct` через `llama.cpp` или `vLLM`) для саммари прямо сейчас. Если справится — отлично, экономим деньги.

---

### Обновленный План Исследования (Этап 1)

#### Роль Агента: Исследователь (Researcher)
**Цель:** Найти работающую связку `ASR + Шумодав + Локальная LLM`, которая дает читаемый результат на CPU.

#### 1. Блок: Распознавание (ASR)
* **База:** `faster-whisper` (модели: `medium`, `large-v3`). Это стандарт де-факто.
* **Альтернатива (если нужно):** `whisper.cpp` (для проверки скорости на чистом CPU).
* **Задача:**
    * Запустить на тестовых файлах (чистый, шумный, тихий спикер).
    * Получить текст + таймкоды.
    * **Проверка качества:**
        1. Посчитать % русских слов (словарь `pymorphy3` или простой список частотных слов).
        2. Если < 0.9 → Fail.
        3. Если >= 0.9 → Выбрать 3 случайных фрагмента по 2 предложения.
        4. **Self-Correction Check:** Агент (ты же просишь его прочитать) оценивает: "Есть ли смысл в этих фразах?". Если бред → Fail.
    * **Вывод:** Список нераспознанных слов (OOV list) для анализа.

#### 2. Блок: Обработка шума (Pre-processing)
* **Инструменты:** `DeepFilterNet`, `RNNoise`, `ffmpeg` (highpass, denoise).
* **Задача:**
    * Взять файл с самым плохим качеством (тихий голос, эхо).
    * Прогнать через каждый метод.
    * Сравнить результат ASR до и после обработки.
    * **Критерий:** Стало ли лучше? Или стало хуже (роботизированный голос)?
    * **Результат:** Рекомендация: использовать или нет.

#### 3. Блок: Таймкоды и Смысловое разбиение (Chunking)
* **Гипотеза:** Жесткое деление по времени плохо. Нужно делить по смыслу.
* **Инструменты:** Локальные эмбеддинги (`sentence-transformers`, `BGE-M3`, `intfloat/e5-mistral`).
* **Задача:**
    * Разбить транскрипт на сегменты.
    * Посчитать косинусное сходство соседних сегментов.
    * Найти точки разрыва (где сходство падает ниже порога, например 0.7).
    * Проверить выборочно: совпадают ли эти точки со сменой темы в тексте? (Агент читает контекст вокруг разрыва).
    * Для таймкодов: сгенерировать осмысленное название для участков с близким смыслом локальной ллм.

#### 4. Блок: Локальное Саммари (LLM)
* **Гипотеза:** `Qwen2.5-7B-Instruct` (или аналог современнее) справится с саммари небольшого совещания на CPU.
* **Инструменты:** `llama.cpp`, `Ollama`, `vLLM` (если есть GPU). Fallback по API: Gemini, NVIDIA (дневные лимиты — только если локально не тянет / слишком долго).
* **HF:** для скачивания моделей можно использовать токен Hugging Face (`HF_TOKEN`).
* **Задача:**
    * Скармливать моделью полученные смысловые чанки (или весь текст, если влезает в контекст).
    * Промпт: "Сделай краткое саммари, выдели решения и действия".
    * **Ограничение:** Время генерации не должно быть астрономическим (например, > 10 мин на 20 мин аудио).
    * **Критерий:** Саммари осмысленно? Есть ли галлюцинации?



# ФОРМАТ ОТЧЕТА
Вернуть JSON файл `research_report.json` со структурой:
{
  "asr_results": [ { "lib": "...", "status": "success/fail", "rw_ratio": 0.95, "oo_words": [...], "sample_check": "ok/bad" } ],
  "denoise_results": [ ... ],
  "chunking_results": [ ... ],
  "llm_summary_results": [ ... ],
  "recommendations": "Итоговый стек для MVP"
}
И текстовый файл `notes.md` с описанием проблем и наблюдений.


