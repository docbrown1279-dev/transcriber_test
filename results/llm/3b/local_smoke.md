# Local smoke — пропуск

`self_check.md` вердикт API: `not_usable`.

По протоколу этапа 3b локальный `Qwen3-8B-Q5_K_M` / llama-cpp не запускался.

Причины API-вердикта — сбой судьи (стиль, затем utterance-часы vs манифест глав), не отсутствие инсайтов. Это не повод обходить правило «local только если usable».
