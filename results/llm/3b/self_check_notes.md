# Self-check: что сделал API и что видно без второй модели

Официальный файл: [`self_check.md`](self_check.md). Вердикт API: `not_usable`. NVIDIA не вызывался.

Судья — тот же `gemini-2.5-flash`, не второй модельный стек. Три прохода:

| Проход | Что пошло не так |
|---|---|
| 1 | Каталог пересказов («это факт, а не цитата»). Обрезан по `maxOutputTokens`. Стиль, не groundedness. |
| 2 | D10 в отчёте был пустой из‑за парсера; судья счёл clock D10 выдумкой. |
| 3 | После починки D10 судья сверил **репликовые** интервалы стенограммы с манифестом **глав**. Манифест так не устроен. Обрезан. |

## Детерминированная проверка (без LLM)

[`structure_check.json`](structure_check.json): `ok=true`.

- 12/12 `clock_json` в инсайтах = манифест `exp_d_chapters.json`
- 12/12 `clock:` в `report.md` = тот же манифест
- 138 `src` попали в метки чанка (точное совпадение или стык соседних реплик)
- заголовки есть; ни один не начинается с «Обсуждение»

`hybrid_asr_gold.md` не собирали (`--gold` / `eval/` запрещены). Self-check читал `full_asr.md`.

## Ручная сверка чисел (не стиль)

Опора в ASR есть у: 30% озеленения; 10 vs 12 кВт; 55–60 м²; 2–3 / 3 месяца; 16 мая; 30–33 м².

Один риск нормализации ASR в новое число: D10 «по пятидесятому» при реплике «что пятьдесят не стоит» (скорее «по десятому»).

Owner в D11 «С меня обратная связь по архитекторам» — в тексте есть, без ФИО.

Локальный 8B не запускался: API-вердикт `not_usable`.
