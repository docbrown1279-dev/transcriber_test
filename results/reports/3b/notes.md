# Этап 3b — инсайты по главам 2b-D

Каркас только **D (Jina, 12 глав)**. C не гоняли. ASR, VAD, нарезка, `results/asr/2/`, `results/llm/3/` не пересчитывались. `eval/` и `.env` не читались.

## Вход

`data/3b_data/chunks_d/D00.md` … `D11.md` собраны `python scripts/asr_json_to_md.py` **без** `--gold` (файлов не было). Источник строк: `full_asr.md` ← `results/asr/2/gigaam_v3_rnnt/meeting_sample.json`. `_unassigned.md` нет: все реплики попали в интервалы D. `hybrid_asr_gold.md` нет.

## Модель

- Gemini `gemini-2.5-flash`, ключ из окружения, текст только. 0 ошибок HTTP. `api_errors.jsonl` не создавался.
- NVIDIA не вызывался (Gemini отвечал с первой попытки).
- Локальный Qwen3-8B: **пропуск** — API self-check = `not_usable`. См. [`local_smoke.md`](../../llm/3b/local_smoke.md).

Вызовы (ориентир, включая ретраи D10 и повторные self-check): ~20, сумма wall ~103 с. Мета: [`run.json`](../../llm/3b/run.json).

## Выход

| Файл | Что |
|---|---|
| [`insights_d/D00.md`](../../llm/3b/insights_d/D00.md) … `D11.md` | `# заголовок` + `clock_json` из чанка + `kind`/`src`/`text` |
| [`report.md`](../../llm/3b/report.md) | Кратко / Решения / Дальше / Открыто / По времени |
| [`self_check.md`](../../llm/3b/self_check.md) | вердикт API `not_usable` |
| [`self_check_notes.md`](../../llm/3b/self_check_notes.md) | почему судья сломался; детерминированная сверка |
| [`structure_check.json`](../../llm/3b/structure_check.json) | 12 clock = манифест, 138 src в чанках |

Заголовки ≤5 слов, без префикса «Обсуждение …». D06 содержит слово «обсуждение» в середине («украшательств») — не шаблон «Обсуждение X».

## Что сломалось в прогоне

1. **D10 title-retry.** Первая строка была «Обсуждение выдачи…». Повтор затёр инсайты, пока скрипт не стали оставлять первый список.
2. **Формат D10.** Модель писала `- question:` и тащила текст реплики в `src`, а не `- kind: question` + только `[…]`. Парсер это выкидывал → «нет инсайтов». Починено; D10 восстановлен из `raw/D10_extract_title_retry.txt` без нового extract.
3. **Self-check.** Тот же Flash судил стиль (проход 1), пустой D10 (проход 2), затем utterance-часы vs манифест глав (проход 3). Это не groundedness. Официальный вердикт всё равно `not_usable`.

## Содержание (на глаз, не эталон)

Держит то, чего не хватало этапу 3: паркинг/точки подключения; 30% озеленения; 10 vs 12 кВт; 55–60 м² и дальневосточная ипотека; 2–3 месяца ресурсник; два пула замечаний архитекторам; корпус 9/10 и «не в корзину / на доработку».

Слабое: D03 тащит ASR-мусор («незакон извращения», «грушек»); D10 «пятидесятому» из «пятьдесят не стоит»; раздел «Дальше» в отчёте раздут (обещания и вопросы как actions); «Решения» пустые — честно.

## Не делали

Новый ASR, VAD, chunking, каркас C, WER, чтение `eval/`, перепись Stage 3 P1/P2 JSON, аудио в API, 14B / GPU.
